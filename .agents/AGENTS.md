# Project Rules

- **Auto-push to GitHub**: Sempre que realizar alterações no código a pedido do usuário, faça automaticamente o commit e push das modificações para o repositório GitHub remoto (`git add .`, `git commit -m "..."`, `git push`).
